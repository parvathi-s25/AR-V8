# Frontend capture upload backend URL (local backend, still used for capture storage)
VITE_CAPTURE_API_BASE_URL=http://localhost:8000

# Animation backend URL — POST image here, receive animated GLB(s) in response
VITE_ANIMATION_API_URL=https://paulita-nonoptimistical-fae.ngrok-free.dev
